## ⚠️  Manual Review Required

This merge request was automatically created because the upstream merge brought in integrations with `dynamic_dataset` and/or `dynamic_namespace` flags.

**What happened:**
- Upstream Elastic integrations were merged
- Auto-fix script detected dynamic flags
- Flags were automatically removed
- Affected integrations marked with (⚠️ SVA RESTRICTED) prefix

**What you need to do:**
1. Review changes in affected integration packages
2. Verify flag removal is correct (check `git diff`)
3. Review `DYNAMIC_DATASET_MODIFICATIONS.md` updates
4. Test affected integrations if critical for production
5. Merge this MR to complete the upstream sync

**Do NOT:**
- Ignore this MR - it blocks future upstream merges
- Close without merging - changes need to be integrated

---

🤖 Auto-generated by merge pipeline
