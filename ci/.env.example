# GitLab CI Environment Variables for Local Testing
#
# Usage:
#   1. Copy this file to .env.local (gitignored)
#   2. Fill in real values from GitLab CI test pipeline
#   3. Source before running: source ci/.env.local && python3 ci/merge_upstream.py
#
# NOTE: Use ci-test-vars pipeline to get real values from GitLab CI

# Git authentication (CRITICAL - needs write access to main branch)
export GITLAB_TOKEN="your-gitlab-token-here"

# GitLab API authentication (for MR operations)
export CI_JOB_TOKEN="your-ci-job-token-here"

# GitLab server details
export CI_SERVER_HOST="codehub.sva.de"
export CI_PROJECT_PATH="sva-soc/soc-infrastructure/elastic/elastic-integrations-soc"

# GitLab API endpoints
export CI_API_V4_URL="https://codehub.sva.de/api/v4"
export CI_PROJECT_ID="your-project-id-here"
export CI_PROJECT_URL="https://codehub.sva.de/sva-soc/soc-infrastructure/elastic/elastic-integrations-soc"

# Pipeline metadata
export CI_PIPELINE_SOURCE="schedule"

# Script configuration (optional overrides)
export FIX_BRANCH="auto-fix-dynamic-flags"
export MR_TARGET="main"
export API_TIMEOUT="30"
