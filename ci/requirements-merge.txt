# Python dependencies for merge pipeline
# Used by ci/merge_upstream.py and ci/remove-dynamic-flags.py

ruamel.yaml>=0.17.0
requests>=2.31.0
PyYAML>=6.0  # For ci/merge_manifest.py (custom merge driver)
